name: prodracerag
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      # API
      - fastapi>=0.110
      - uvicorn[standard]>=0.27
      - pydantic>=2.0
      - requests>=2.31

      # Retrieval / RAG
      - chromadb>=0.5.0
      - rank-bm25>=0.2.2
      - jieba>=0.42.1
      - numpy>=1.26

      # Reporting
      - openpyxl>=3.1

      # Optional: tests / CI
      - pytest>=8.0
      - ruff>=0.4.0
